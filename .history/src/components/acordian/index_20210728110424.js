import React from 'react'
import { Container } from '../jumbotron/styles/jumbotron'

export default function Acordian({ children, ...restProps }) {
  return (
    <Container { ...restProps }>
      
    </Container>
  )
}
